{
  "devDependencies": {
    "cypress": "^12.5.1",
    "cypress-xpath": "^2.0.1"
  },
  "scripts": {
    "cy:run:chrome": "cypress run -b chrome",
    "cy:run:firefox": "cypress run -b firefox",
    "start": "npx cypress open"
  }
}
